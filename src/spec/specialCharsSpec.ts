import { renderComponent as render } from "./makeInlineComponent";
import { defaultOptions } from "../utils/options";

xdescribe("special characters in attributes", ()=> {
   it("works", ()=>{          
   });
});

xdescribe("special characters in text", ()=> {
   it("works", ()=>{          
   });
});
